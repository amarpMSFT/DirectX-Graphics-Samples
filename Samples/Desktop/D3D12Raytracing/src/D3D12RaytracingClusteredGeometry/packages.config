<?xml version="1.0" encoding="utf-8"?>
<!--
  No Microsoft.Direct3D.D3D12 NuGet here: this sample uses an experimental D3D12 build
  hooked in via ExperimentalD3D12.props. Drop this file (and the .props file) and
  re-add the standard nuget references the way sibling samples do once a real
  Agility SDK package supporting DXR2 ships.
-->
<packages>
</packages>
